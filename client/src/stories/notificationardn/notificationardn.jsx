import React from 'react'

import Notifications from '../../app/components/notifications/notifications'

export default function NotificationAccordion({ data }) {
  return (
    <div>
      <Notifications data={data} />
    </div>
  )
}
